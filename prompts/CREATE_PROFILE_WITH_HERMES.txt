Create and configure a new specialized Hermes profile named `restaurant-cost-controller`.

Prefer installing the extracted package as a local profile distribution:

hermes profile install "<PATH-TO-EXTRACTED-PACKAGE>/restaurant-cost-controller" --alias

If local distribution installation is unavailable, create it manually:

hermes profile create restaurant-cost-controller --clone --description "Processes restaurant invoices, tracks vendor item prices and purchasing costs, compares costs with sales, and produces auditable manager reports."

Then:

1. Use the package SOUL.md for the profile identity.
2. Install the package skill directory `skills/productivity/restaurant-cost-control`.
3. Use HERMES.md as the project instructions.
4. Keep this profile’s memories and sessions isolated.
5. Do not import unrelated personal or game-development memories.
6. Confirm OCR/document and Google Workspace skills are available.
7. Guide me through Google Workspace OAuth only when needed.
8. Use `assets/Restaurant_Cost_Tracker_Template.xlsx`.
9. Create or connect Upload Invoices, Processed Invoices, Needs Review, Monthly Sales, Original Documents, and Exports folders.
10. Ask for missing restaurant-specific settings only when required.
11. Process invoices using the canonical schema and included normalizer.
12. Match products by vendor+SKU, approved alias, vendor+normalized description, then new item requiring review.
13. Never silently guess uncertain values.
14. Never call sales minus purchases net profit.
15. Show proposed schedules before creating invoice-intake, nightly-validation, weekly-summary, and month-end jobs.
16. Run the sample-data dry test.
17. Report the profile path, installed skills, workbook location, storage connection, scheduled jobs, dry-run results, and unresolved setup items.

Do not modify or delete any existing profile.
